export type RootStackParamList = {
  Home: undefined; // Assuming Home doesn't take any params
  // Add other routes here if needed
  // Survey: { id: string }; // Example of a route with parameters
};
